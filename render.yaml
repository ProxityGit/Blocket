databases:
  - name: blocket-db
    databaseName: blocket
    user: blocket_user
    plan: free

services:

  # Backend API (Node.js + Express)
  - type: web
    name: blocket-api
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://blocket-frontend.onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: blocket-db
          property: connectionString
      - key: CLOUDINARY_CLOUD_NAME
        sync: false
      - key: CLOUDINARY_API_KEY
        sync: false
      - key: CLOUDINARY_API_SECRET
        sync: false
    healthCheckPath: /api/health

  # Frontend (React + Vite)
  - type: web
    name: blocket-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://blocket-api-7ku5.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
